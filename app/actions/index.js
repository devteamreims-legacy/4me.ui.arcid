import * as ArcidQuery from './arcidQuery';
import * as Profile from './profile';
import * as Errors from './errors';

const defaults = {
  ArcidQuery,
  Profile,
  Errors
};

export default defaults;